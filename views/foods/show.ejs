<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>
        <%= food.name %>
    </title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
        <h1>
            <%= food.name %>
        </h1>
        <p>Expiration: <%= food.expiration.toDateString() %>
        </p>
        <p>Quantity: <%= food.quantity %>
        </p>
        <p>Is Favorite: <%= food.isFavorite ? 'Yes' : 'No' %>
        </p>
        <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit">Edit</a>
        <br>
        <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE" method="POST">
            <button type="submit">Delete</button>
        </form>
</body>

</html>